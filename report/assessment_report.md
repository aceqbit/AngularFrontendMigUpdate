# Assessment Report: Angular v20 → v21

Date: 2026-05-22

Repository path: d:/AngularFrontendMigUpdate

## Summary
- Current `@angular/*` packages: `^20.0.0` (see `package.json`).
- Dev tooling: `@angular/cli` and build tools targeting `^20.0.0`.
- TypeScript: `^5.9.0` (devDependency).
- RxJS: `~7.8.0`.
- Total Angular components discovered under `src/app/components`: 19

## Timer / Async Mutation Inventory
- `src/app/components/dashboard-widgets/dashboard-widgets.component.ts` — uses `setInterval` and calls `ChangeDetectorRef.markForCheck()` (OK).
- `src/app/components/file-dropzone-lab/file-dropzone-lab.component.ts` — simulated upload timers with `markForCheck()` (OK).
- `src/app/components/resource-monitor/resource-monitor.component.ts` — simulation interval with `markForCheck()` (OK).

## High-risk Areas
- Timer-based components were identified and already include `ChangeDetectorRef.markForCheck()` calls; low risk for change-detection regressions.
- Potential third-party peer-dependency conflicts may occur when bumping to Angular v21 — plan to run `ng update` and document any `--force` usage.
- CSS budgets and styling warnings were previously observed in earlier migration runs; plan a smoke visual check after build.

## Recommended Immediate Actions
1. Create a migration branch and tag current `v20-stable` (pre-migration snapshot).
2. Run `npm install` to ensure current deps are installed.
3. Run `npx @angular/cli@21 update @angular/core@21 @angular/cli@21 --force` (or `ng update` equivalent) and capture migration output.
4. Run production build and unit tests: `npx ng build --configuration=production` and `npx ng test -- --watch=false`.
5. Address any build/test failures; iterate targeted fixes for failing specs.

## Outputs generated by the automated run
- `report/implementation_log.md` — append-only timeline of actions.
- `report/test_report.md` — will contain test results after execution.
- `report/css_report.md` — will contain any CSS modernization notes.

---

Recorded-by: migration master agent
